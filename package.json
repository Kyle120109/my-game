{
  "name": "bike-mountain-rush",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/**/*.test.js",
    "lint": "node scripts/lint.mjs",
    "check": "npm run lint && npm test && npm run build",
    "audit": "node scripts/audit.mjs"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "puppeteer": "^24.37.5",
    "vite": "^7.1.3"
  }
}
