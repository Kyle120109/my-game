# Urban8 交汇立交重做验收标准（参考图桥上跨）

## 范围
- 仅重做 `urban8`（第3章）及其在 `src/game/world.js` 的必要构建逻辑。
- 不改关卡 schema 命名；沿用现有字段：`path/checkpoints/ramps/boosts/items/bounds/palette/mountainField/heightFn` 等。

## 必须通过（Task 10 前逐项勾选）
- [x] 交汇结构为“桥上跨 + 地面道路通过”，禁止深沟/箱涵盖板式下穿观感。
- [x] 两堵碍事高墙/夹墙走廊被删除或重构，交汇区视线开阔、容错提升。
- [x] 交汇区无穿模/虚模/透明挡路碰撞；视觉与碰撞一致。
- [x] 匝道为长缓坡上桥/下桥，桥顶相对平直，坡度过渡自然。
- [x] 桥体工程语言成立：薄桥面、桥侧护栏/矮挡墙、2–4 根桥墩、桥下净空明确。
- [x] 路线可读不依赖 checkpoint 圈：有地面导流线/箭头 + 方向牌/灯箱节奏。
- [x] 交汇区道路去规整化：宏观仍是“8”，微观增加弯折、导流与层次。
- [x] 交汇区边界连续且无碎片陷阱；贴边测试不过度刮停。
- [x] 周边形成贴路连续街区，并补齐绿化与街道设施，避免“灰地+孤岛盒子”。
- [x] 最终回归通过：`npm run check` 通过；`fullAudit('urban8')` 通过；`freeCamAuditAll` 中 urban8 为 0 遮挡；`fullMapSweep('urban8')` 零问题通过（全图汇总受其他关卡影响）。
